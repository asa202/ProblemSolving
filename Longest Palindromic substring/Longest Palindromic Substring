using namespace std;
#include <string>

bool checkPalindrome(string str , int i , int j){
	int mid = ((i+j)/2) + 1;
	for(; i < mid ; i++){
		if(str[i] != str[j]){
			return false;
		}
		j--;
	}
	return true;
}

string longestPalindromicSubstring(string str) {
  // Write your code here.
	string max = "";
	for(int i = 0 ; i < str.size(); i++){
		if(max.size() > str.size() - i + 1){
					break;
				}
		for(int j = str.size()-1; j > i; j--){
			if(str[i] == str[j]){
				if(max.size() > j - i + 1){
					break;
				}
				if(checkPalindrome(str, i, j)){
					if(max.size() < j - i + 1){
						max = str.substr(i, j - i +1);
					}
					break;
				}
			}
		}
	}
	if(max.size() == 0){
		return str.substr(0,1);
	}
  return max;
}
